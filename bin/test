#!/bin/bash

set -e
set -x
set -u

if ! which go 2>/dev/null; then
  echo "Go is not installed."
  exit 1
fi

go fmt ./riak-cs-service/...

CF_COLOR=false CF_VERBOSE_OUTPUT=true \
  ginkgo -v -slowSpecThreshold=300 "$@" ./riak-cs-service
